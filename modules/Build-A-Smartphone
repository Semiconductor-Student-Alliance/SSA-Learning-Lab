<!-- SSA BUILD-A-SMARTPHONE INTERACTIVE -->
<div id="ssa-phone-builder">
  <div class="ssa-header">
    <p class="ssa-eyebrow">SSA LEARNING LAB</p>
    <h2>Build a Smartphone</h2>
    <p>
      Drag each component into the correct location inside the phone.
      On a phone or tablet, tap a component and then tap its destination.
    </p>
  </div>

  <div class="ssa-progress-section">
    <div class="ssa-progress-label">
      <span>Progress</span>
      <strong id="ssa-progress-text">0 of 6</strong>
    </div>

    <div class="ssa-progress-track">
      <div id="ssa-progress-bar"></div>
    </div>
  </div>

  <div class="ssa-game-layout">

    <!-- COMPONENT TRAY -->
    <section class="ssa-panel ssa-components-panel">
      <h3>Components</h3>
      <p class="ssa-panel-instruction">
        Select or drag a component.
      </p>

      <div id="ssa-component-tray">

        <button
          class="ssa-component"
          draggable="true"
          data-component="camera"
          type="button"
        >
          <span class="ssa-component-icon">◉</span>
          <span>
            <strong>Camera Sensor</strong>
            <small>Captures light</small>
          </span>
        </button>

        <button
          class="ssa-component"
          draggable="true"
          data-component="processor"
          type="button"
        >
          <span class="ssa-component-icon">CPU</span>
          <span>
            <strong>Processor</strong>
            <small>Runs calculations</small>
          </span>
        </button>

        <button
          class="ssa-component"
          draggable="true"
          data-component="memory"
          type="button"
        >
          <span class="ssa-component-icon">RAM</span>
          <span>
            <strong>Memory</strong>
            <small>Stores active data</small>
          </span>
        </button>

        <button
          class="ssa-component"
          draggable="true"
          data-component="pmic"
          type="button"
        >
          <span class="ssa-component-icon">⚡</span>
          <span>
            <strong>Power Chip</strong>
            <small>Controls voltage</small>
          </span>
        </button>

        <button
          class="ssa-component"
          draggable="true"
          data-component="battery"
          type="button"
        >
          <span class="ssa-component-icon">▰</span>
          <span>
            <strong>Battery</strong>
            <small>Stores energy</small>
          </span>
        </button>

        <button
          class="ssa-component"
          draggable="true"
          data-component="antenna"
          type="button"
        >
          <span class="ssa-component-icon">⌁</span>
          <span>
            <strong>Antenna</strong>
            <small>Sends wireless signals</small>
          </span>
        </button>

      </div>
    </section>

    <!-- PHONE -->
    <section class="ssa-phone-section" aria-label="Smartphone assembly area">
      <div class="ssa-phone-shell">

        <div class="ssa-phone-speaker"></div>

        <button
          class="ssa-drop-zone ssa-camera-zone"
          data-accepts="camera"
          type="button"
          aria-label="Camera sensor location"
        >
          <span>Camera</span>
        </button>

        <button
          class="ssa-drop-zone ssa-antenna-zone"
          data-accepts="antenna"
          type="button"
          aria-label="Antenna location"
        >
          <span>Antenna</span>
        </button>

        <div class="ssa-board-label">MAIN LOGIC BOARD</div>

        <button
          class="ssa-drop-zone ssa-processor-zone"
          data-accepts="processor"
          type="button"
          aria-label="Processor location"
        >
          <span>Processor</span>
        </button>

        <button
          class="ssa-drop-zone ssa-memory-zone"
          data-accepts="memory"
          type="button"
          aria-label="Memory location"
        >
          <span>Memory</span>
        </button>

        <button
          class="ssa-drop-zone ssa-pmic-zone"
          data-accepts="pmic"
          type="button"
          aria-label="Power management chip location"
        >
          <span>Power</span>
        </button>

        <button
          class="ssa-drop-zone ssa-battery-zone"
          data-accepts="battery"
          type="button"
          aria-label="Battery location"
        >
          <span>Battery</span>
        </button>

        <div class="ssa-home-indicator"></div>
      </div>

      <p id="ssa-selection-message" aria-live="polite">
        Choose a component to begin.
      </p>
    </section>

    <!-- INFORMATION PANEL -->
    <section class="ssa-panel ssa-info-panel">
      <h3>What It Does</h3>

      <div id="ssa-info-content">
        <div class="ssa-info-placeholder">?</div>
        <h4>Select a component</h4>
        <p>
          Learn how semiconductors and electronic components work together
          inside a smartphone.
        </p>
      </div>

      <div class="ssa-controls">
        <button id="ssa-hint-button" type="button">Show Hint</button>
        <button id="ssa-reset-button" type="button">Reset</button>
      </div>
    </section>

  </div>

  <!-- COMPLETION MESSAGE -->
  <div id="ssa-complete-message" hidden>
    <div class="ssa-complete-icon">✓</div>
    <div>
      <h3>Smartphone Complete!</h3>
      <p>
        You connected the sensing, computing, memory, power, energy, and
        communication systems inside a smartphone.
      </p>
    </div>
  </div>
</div>

<style>
  #ssa-phone-builder {
    --ssa-navy: #10253f;
    --ssa-blue: #1769aa;
    --ssa-light-blue: #e9f4fb;
    --ssa-green: #23895d;
    --ssa-red: #c74646;
    --ssa-gold: #f1b642;
    --ssa-border: #d7e0e8;
    --ssa-text: #263746;
    --ssa-muted: #667786;
    --ssa-white: #ffffff;

    max-width: 1180px;
    margin: 30px auto;
    padding: 30px;
    border: 1px solid var(--ssa-border);
    border-radius: 24px;
    background:
      radial-gradient(circle at top right, #edf8ff 0, transparent 30%),
      var(--ssa-white);
    color: var(--ssa-text);
    box-shadow: 0 18px 45px rgba(16, 37, 63, 0.1);
    font-family: Arial, Helvetica, sans-serif;
    box-sizing: border-box;
  }

  #ssa-phone-builder * {
    box-sizing: border-box;
  }

  .ssa-header {
    max-width: 760px;
    margin-bottom: 24px;
  }

  .ssa-eyebrow {
    margin: 0 0 8px;
    color: var(--ssa-blue);
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 0.14em;
  }

  .ssa-header h2 {
    margin: 0 0 10px;
    color: var(--ssa-navy);
    font-size: clamp(30px, 5vw, 48px);
    line-height: 1.05;
  }

  .ssa-header p {
    margin: 0;
    color: var(--ssa-muted);
    font-size: 17px;
    line-height: 1.6;
  }

  .ssa-progress-section {
    margin-bottom: 28px;
  }

  .ssa-progress-label {
    display: flex;
    justify-content: space-between;
    margin-bottom: 8px;
    font-size: 14px;
  }

  .ssa-progress-track {
    width: 100%;
    height: 12px;
    overflow: hidden;
    border-radius: 999px;
    background: #e8edf1;
  }

  #ssa-progress-bar {
    width: 0%;
    height: 100%;
    border-radius: inherit;
    background: var(--ssa-green);
    transition: width 0.35s ease;
  }

  .ssa-game-layout {
    display: grid;
    grid-template-columns: minmax(220px, 1fr) minmax(280px, 1.15fr) minmax(220px, 1fr);
    gap: 24px;
    align-items: start;
  }

  .ssa-panel {
    min-height: 490px;
    padding: 22px;
    border: 1px solid var(--ssa-border);
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.93);
  }

  .ssa-panel h3 {
    margin: 0 0 6px;
    color: var(--ssa-navy);
    font-size: 21px;
  }

  .ssa-panel-instruction {
    margin: 0 0 18px;
    color: var(--ssa-muted);
    font-size: 14px;
  }

  #ssa-component-tray {
    display: grid;
    gap: 11px;
  }

  .ssa-component {
    display: flex;
    width: 100%;
    align-items: center;
    gap: 12px;
    padding: 12px;
    border: 2px solid transparent;
    border-radius: 13px;
    background: #f5f8fa;
    color: var(--ssa-text);
    text-align: left;
    cursor: grab;
    transition:
      transform 0.18s ease,
      border-color 0.18s ease,
      background 0.18s ease,
      opacity 0.18s ease;
  }

  .ssa-component:hover {
    transform: translateY(-2px);
    border-color: var(--ssa-blue);
    background: var(--ssa-light-blue);
  }

  .ssa-component:focus-visible,
  .ssa-drop-zone:focus-visible,
  .ssa-controls button:focus-visible {
    outline: 3px solid rgba(23, 105, 170, 0.3);
    outline-offset: 3px;
  }

  .ssa-component.ssa-selected {
    border-color: var(--ssa-blue);
    background: var(--ssa-light-blue);
  }

  .ssa-component.ssa-placed {
    opacity: 0.35;
    cursor: default;
    pointer-events: none;
  }

  .ssa-component-icon {
    display: grid;
    flex: 0 0 46px;
    width: 46px;
    height: 46px;
    place-items: center;
    border-radius: 11px;
    background: var(--ssa-navy);
    color: white;
    font-size: 13px;
    font-weight: 800;
  }

  .ssa-component strong,
  .ssa-component small {
    display: block;
  }

  .ssa-component strong {
    margin-bottom: 3px;
    font-size: 14px;
  }

  .ssa-component small {
    color: var(--ssa-muted);
    font-size: 12px;
  }

  .ssa-phone-section {
    text-align: center;
  }

  .ssa-phone-shell {
    position: relative;
    width: min(100%, 330px);
    height: 590px;
    margin: 0 auto;
    overflow: hidden;
    border: 9px solid #17202a;
    border-radius: 46px;
    background:
      linear-gradient(145deg, rgba(255,255,255,0.75), rgba(222,233,241,0.8)),
      repeating-linear-gradient(
        90deg,
        transparent 0,
        transparent 28px,
        rgba(16,37,63,0.025) 29px,
        rgba(16,37,63,0.025) 30px
      );
    box-shadow:
      inset 0 0 0 2px #71808b,
      0 18px 30px rgba(16, 37, 63, 0.18);
  }

  .ssa-phone-speaker {
    position: absolute;
    top: 15px;
    left: 50%;
    width: 76px;
    height: 7px;
    border-radius: 999px;
    background: #17202a;
    transform: translateX(-50%);
  }

  .ssa-home-indicator {
    position: absolute;
    bottom: 13px;
    left: 50%;
    width: 92px;
    height: 5px;
    border-radius: 999px;
    background: #17202a;
    transform: translateX(-50%);
  }

  .ssa-board-label {
    position: absolute;
    top: 145px;
    left: 31px;
    padding: 6px 8px;
    border-radius: 6px;
    background: rgba(16, 37, 63, 0.08);
    color: var(--ssa-muted);
    font-size: 9px;
    font-weight: 800;
    letter-spacing: 0.08em;
  }

  .ssa-drop-zone {
    position: absolute;
    display: grid;
    place-items: center;
    border: 2px dashed #8393a0;
    border-radius: 11px;
    background: rgba(255, 255, 255, 0.75);
    color: #657786;
    cursor: pointer;
    transition:
      border-color 0.18s ease,
      background 0.18s ease,
      transform 0.18s ease;
  }

  .ssa-drop-zone span {
    padding: 5px;
    font-size: 11px;
    font-weight: 700;
  }

  .ssa-drop-zone.ssa-drag-over {
    border-color: var(--ssa-blue);
    background: var(--ssa-light-blue);
    transform: scale(1.04);
  }

  .ssa-drop-zone.ssa-wrong {
    border-color: var(--ssa-red);
    background: #fff0f0;
    animation: ssa-shake 0.3s ease;
  }

  .ssa-drop-zone.ssa-hint {
    border-color: var(--ssa-gold);
    background: #fff8df;
    animation: ssa-pulse 0.9s ease infinite alternate;
  }

  .ssa-drop-zone.ssa-filled {
    border-style: solid;
    border-color: var(--ssa-green);
    background: #e9f8f0;
    color: #12653f;
    cursor: default;
  }

  .ssa-camera-zone {
    top: 48px;
    left: 26px;
    width: 92px;
    height: 78px;
  }

  .ssa-antenna-zone {
    top: 48px;
    right: 27px;
    width: 82px;
    height: 78px;
  }

  .ssa-processor-zone {
    top: 180px;
    left: 34px;
    width: 117px;
    height: 90px;
  }

  .ssa-memory-zone {
    top: 180px;
    right: 34px;
    width: 105px;
    height: 90px;
  }

  .ssa-pmic-zone {
    top: 286px;
    left: 34px;
    width: 105px;
    height: 74px;
  }

  .ssa-battery-zone {
    top: 383px;
    left: 50%;
    width: 205px;
    height: 142px;
    transform: translateX(-50%);
  }

  .ssa-battery-zone.ssa-drag-over {
    transform: translateX(-50%) scale(1.03);
  }

  .ssa-battery-zone.ssa-hint {
    transform: translateX(-50%);
  }

  #ssa-selection-message {
    min-height: 24px;
    margin: 16px 0 0;
    color: var(--ssa-muted);
    font-size: 14px;
  }

  .ssa-info-panel {
    display: flex;
    flex-direction: column;
  }

  #ssa-info-content {
    flex: 1;
    padding-top: 16px;
  }

  .ssa-info-placeholder {
    display: grid;
    width: 70px;
    height: 70px;
    margin-bottom: 18px;
    place-items: center;
    border-radius: 18px;
    background: var(--ssa-light-blue);
    color: var(--ssa-blue);
    font-size: 34px;
    font-weight: 800;
  }

  #ssa-info-content h4 {
    margin: 0 0 8px;
    color: var(--ssa-navy);
    font-size: 22px;
  }

  #ssa-info-content p {
    margin: 0;
    color: var(--ssa-muted);
    font-size: 15px;
    line-height: 1.6;
  }

  .ssa-info-tag {
    display: inline-block;
    margin-top: 16px;
    padding: 7px 10px;
    border-radius: 999px;
    background: var(--ssa-light-blue);
    color: var(--ssa-blue);
    font-size: 12px;
    font-weight: 800;
  }

  .ssa-controls {
    display: flex;
    gap: 10px;
    margin-top: 24px;
  }

  .ssa-controls button {
    flex: 1;
    padding: 11px 12px;
    border: 0;
    border-radius: 10px;
    font-weight: 700;
    cursor: pointer;
  }

  #ssa-hint-button {
    background: var(--ssa-blue);
    color: white;
  }

  #ssa-reset-button {
    background: #edf1f4;
    color: var(--ssa-navy);
  }

  #ssa-complete-message {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-top: 24px;
    padding: 20px;
    border: 2px solid var(--ssa-green);
    border-radius: 16px;
    background: #eaf8f1;
  }

  #ssa-complete-message[hidden] {
    display: none;
  }

  .ssa-complete-icon {
    display: grid;
    flex: 0 0 58px;
    width: 58px;
    height: 58px;
    place-items: center;
    border-radius: 50%;
    background: var(--ssa-green);
    color: white;
    font-size: 31px;
    font-weight: 800;
  }

  #ssa-complete-message h3 {
    margin: 0 0 5px;
    color: #12653f;
  }

  #ssa-complete-message p {
    margin: 0;
    line-height: 1.5;
  }

  @keyframes ssa-shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-6px); }
    75% { transform: translateX(6px); }
  }

  @keyframes ssa-pulse {
    from { box-shadow: 0 0 0 0 rgba(241, 182, 66, 0.25); }
    to { box-shadow: 0 0 0 10px rgba(241, 182, 66, 0); }
  }

  @media (max-width: 900px) {
    .ssa-game-layout {
      grid-template-columns: 1fr 1fr;
    }

    .ssa-phone-section {
      grid-row: span 2;
    }

    .ssa-info-panel {
      min-height: auto;
    }
  }

  @media (max-width: 680px) {
    #ssa-phone-builder {
      margin: 16px auto;
      padding: 20px 14px;
      border-radius: 18px;
    }

    .ssa-game-layout {
      grid-template-columns: 1fr;
    }

    .ssa-phone-section {
      grid-row: auto;
      order: 2;
    }

    .ssa-components-panel {
      order: 1;
      min-height: auto;
    }

    .ssa-info-panel {
      order: 3;
      min-height: auto;
    }

    #ssa-component-tray {
      grid-template-columns: 1fr 1fr;
    }

    .ssa-component {
      align-items: flex-start;
      padding: 9px;
    }

    .ssa-component-icon {
      flex-basis: 38px;
      width: 38px;
      height: 38px;
      font-size: 11px;
    }

    .ssa-component strong {
      font-size: 12px;
    }

    .ssa-component small {
      font-size: 10px;
    }

    .ssa-phone-shell {
      height: 560px;
    }
  }
</style>

<script>
(function () {
  const builder = document.getElementById("ssa-phone-builder");

  if (!builder) return;

  const components = Array.from(
    builder.querySelectorAll(".ssa-component")
  );

  const dropZones = Array.from(
    builder.querySelectorAll(".ssa-drop-zone")
  );

  const progressText = builder.querySelector("#ssa-progress-text");
  const progressBar = builder.querySelector("#ssa-progress-bar");
  const selectionMessage = builder.querySelector(
    "#ssa-selection-message"
  );
  const infoContent = builder.querySelector("#ssa-info-content");
  const hintButton = builder.querySelector("#ssa-hint-button");
  const resetButton = builder.querySelector("#ssa-reset-button");
  const completeMessage = builder.querySelector(
    "#ssa-complete-message"
  );

  const totalComponents = components.length;

  let selectedComponent = null;
  let draggedComponent = null;
  let completedCount = 0;

  const componentInformation = {
    camera: {
      title: "CMOS Camera Sensor",
      icon: "◉",
      description:
        "The image sensor converts incoming light into electrical signals. Millions of tiny light-sensitive pixels work together to create a digital photograph.",
      category: "Sensing"
    },
    processor: {
      title: "Application Processor",
      icon: "CPU",
      description:
        "The processor runs applications, performs calculations, processes images, and coordinates many of the other systems inside the phone.",
      category: "Computing"
    },
    memory: {
      title: "Memory",
      icon: "RAM",
      description:
        "Memory temporarily holds information that the processor needs quickly. More memory helps a phone run several applications at the same time.",
      category: "Data Storage"
    },
    pmic: {
      title: "Power-Management IC",
      icon: "⚡",
      description:
        "The power-management chip converts battery energy into the different voltages required by the processor, memory, camera, display, and radios.",
      category: "Power Electronics"
    },
    battery: {
      title: "Lithium-Ion Battery",
      icon: "▰",
      description:
        "The battery stores chemical energy and supplies electrical energy to the phone. Engineers balance capacity, charging speed, size, safety, and cost.",
      category: "Energy Storage"
    },
    antenna: {
      title: "Wireless Antenna",
      icon: "⌁",
      description:
        "The antenna sends and receives electromagnetic signals for cellular service, Wi-Fi, Bluetooth, GPS, and other wireless connections.",
      category: "Communication"
    }
  };

  function showInformation(componentName) {
    const information = componentInformation[componentName];

    if (!information) return;

    infoContent.innerHTML = `
      <div class="ssa-info-placeholder">${information.icon}</div>
      <h4>${information.title}</h4>
      <p>${information.description}</p>
      <span class="ssa-info-tag">${information.category}</span>
    `;
  }

  function clearSelection() {
    components.forEach(function (component) {
      component.classList.remove("ssa-selected");
    });

    selectedComponent = null;
  }

  function selectComponent(component) {
    if (component.classList.contains("ssa-placed")) return;

    clearSelection();

    selectedComponent = component;
    component.classList.add("ssa-selected");

    const componentName = component.dataset.component;
    const information = componentInformation[componentName];

    selectionMessage.textContent =
      information.title + " selected. Now choose its location.";

    showInformation(componentName);
  }

  function updateProgress() {
    progressText.textContent =
      completedCount + " of " + totalComponents;

    const percentage =
      (completedCount / totalComponents) * 100;

    progressBar.style.width = percentage + "%";

    if (completedCount === totalComponents) {
      selectionMessage.textContent =
        "Excellent work. You completed the smartphone.";

      completeMessage.hidden = false;
      hintButton.disabled = true;
      hintButton.style.opacity = "0.5";
    }
  }

  function showIncorrect(zone, componentName) {
    const information = componentInformation[componentName];

    zone.classList.add("ssa-wrong");

    selectionMessage.textContent =
      information.title + " does not belong there. Try another location.";

    window.setTimeout(function () {
      zone.classList.remove("ssa-wrong");
    }, 500);
  }

  function placeComponent(component, zone) {
    if (!component || !zone) return;

    const componentName = component.dataset.component;
    const acceptedComponent = zone.dataset.accepts;

    if (componentName !== acceptedComponent) {
      showIncorrect(zone, componentName);
      return;
    }

    if (zone.classList.contains("ssa-filled")) return;

    const information = componentInformation[componentName];

    zone.classList.remove("ssa-hint", "ssa-drag-over");
    zone.classList.add("ssa-filled");
    zone.innerHTML = `
      <span>
        ${information.icon}<br>
        ${information.title}
      </span>
    `;

    zone.disabled = true;
    component.classList.add("ssa-placed");
    component.classList.remove("ssa-selected");
    component.setAttribute("aria-disabled", "true");

    completedCount += 1;
    selectedComponent = null;
    draggedComponent = null;

    selectionMessage.textContent =
      information.title + " placed correctly.";

    showInformation(componentName);
    updateProgress();
  }

  components.forEach(function (component) {
    component.addEventListener("click", function () {
      selectComponent(component);
    });

    component.addEventListener("dragstart", function (event) {
      if (component.classList.contains("ssa-placed")) {
        event.preventDefault();
        return;
      }

      draggedComponent = component;
      selectComponent(component);

      event.dataTransfer.effectAllowed = "move";
      event.dataTransfer.setData(
        "text/plain",
        component.dataset.component
      );
    });

    component.addEventListener("dragend", function () {
      draggedComponent = null;

      dropZones.forEach(function (zone) {
        zone.classList.remove("ssa-drag-over");
      });
    });
  });

  dropZones.forEach(function (zone) {
    zone.addEventListener("click", function () {
      if (selectedComponent) {
        placeComponent(selectedComponent, zone);
      } else if (!zone.classList.contains("ssa-filled")) {
        selectionMessage.textContent =
          "Select a component first, then choose its location.";
      }
    });

    zone.addEventListener("dragover", function (event) {
      event.preventDefault();

      if (!zone.classList.contains("ssa-filled")) {
        zone.classList.add("ssa-drag-over");
      }
    });

    zone.addEventListener("dragleave", function () {
      zone.classList.remove("ssa-drag-over");
    });

    zone.addEventListener("drop", function (event) {
      event.preventDefault();
      zone.classList.remove("ssa-drag-over");

      if (draggedComponent) {
        placeComponent(draggedComponent, zone);
      }
    });
  });

  hintButton.addEventListener("click", function () {
    dropZones.forEach(function (zone) {
      zone.classList.remove("ssa-hint");
    });

    let componentToHint = selectedComponent;

    if (!componentToHint) {
      componentToHint = components.find(function (component) {
        return !component.classList.contains("ssa-placed");
      });

      if (componentToHint) {
        selectComponent(componentToHint);
      }
    }

    if (!componentToHint) return;

    const componentName = componentToHint.dataset.component;
    const matchingZone = dropZones.find(function (zone) {
      return zone.dataset.accepts === componentName;
    });

    if (matchingZone) {
      matchingZone.classList.add("ssa-hint");

      selectionMessage.textContent =
        "Hint: look for the highlighted location.";

      window.setTimeout(function () {
        matchingZone.classList.remove("ssa-hint");
      }, 2500);
    }
  });

  resetButton.addEventListener("click", function () {
    completedCount = 0;
    selectedComponent = null;
    draggedComponent = null;

    components.forEach(function (component) {
      component.classList.remove(
        "ssa-selected",
        "ssa-placed"
      );

      component.removeAttribute("aria-disabled");
    });

    dropZones.forEach(function (zone) {
      const label =
        zone.getAttribute("aria-label")
          .replace(" location", "")
          .replace(" chip", "");

      zone.classList.remove(
        "ssa-filled",
        "ssa-hint",
        "ssa-wrong",
        "ssa-drag-over"
      );

      zone.disabled = false;
      zone.innerHTML = "<span>" + label + "</span>";
    });

    infoContent.innerHTML = `
      <div class="ssa-info-placeholder">?</div>
      <h4>Select a component</h4>
      <p>
        Learn how semiconductors and electronic components work together
        inside a smartphone.
      </p>
    `;

    completeMessage.hidden = true;
    hintButton.disabled = false;
    hintButton.style.opacity = "1";

    selectionMessage.textContent =
      "Choose a component to begin.";

    updateProgress();
  });

  updateProgress();
})();
</script>
